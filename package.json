{
  "name": "aws-sdk-browser-builder-server",
  "description": "AWS SDK for JavaScript browser building server",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "git://github.com/aws/aws-sdk-js#v2.0.0-rc.20",
    "express": "*",
    "graceful-fs": "*",
    "async": "*",
    "compression": "*",
    "morgan": "*"
  },
  "devDependencies": {
    "mocha": "*",
    "chai": "*",
    "coffee-script": "1.6.x",
    "supertest": "0.9.0",
    "istanbul": "*",
    "eslint": "*"
  },
  "scripts" : {
    "test": "npm run-script lint && npm run-script unit",
    "unit": "mocha --compilers coffee:coffee-script -s 1000 -t 10000",
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha -- --compilers coffee:coffee-script -s 1000 -t 10000",
    "lint": "eslint *.js"
  }
}
