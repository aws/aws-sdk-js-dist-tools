{
  "name": "aws-sdk-browser-builder",
  "description": "AWS SDK for JavaScript browser building tools",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "2.x",
    "browserify": "3.x",
    "uglify-js": "2.x",
    "express": "3.x"
  },
  "devDependencies": {
    "async": "*",
    "graceful-fs": "*",
    "mocha": "*",
    "chai": "*",
    "coffee-script": "1.6.x",
    "supertest": "*",
    "istanbul": "*"
  },
  "scripts" : {
    "test": "mocha --compilers coffee:coffee-script -s 1000 -t 10000",
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha -- --compilers coffee:coffee-script -s 1000 -t 10000"
  }
}
